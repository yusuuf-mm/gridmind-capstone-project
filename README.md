# gridmind-capstone-project
This project demonstrates deploying a **static web application** using **AWS S3** and **CloudFront**. The goal was to host a simple but fully functional website globally using a CDN with secure access policies.
